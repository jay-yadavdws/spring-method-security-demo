### Spring Method Security With Roles & Permission

* Key Considerations :

We have configured 2 USERS for the application using `@Bean UserDetailsService` and its `InMemoryUserDetailsManager.java` implementation